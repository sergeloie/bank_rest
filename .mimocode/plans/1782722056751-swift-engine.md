# Remaining Cleanup + Card Masking

## Что уже сделано (пользователем)
- ✅ Удалён `Length` импорт из `CardProperties`
- ✅ Удалены `CARD_LENGTH`, `BIN_LENGTH` из `CardNumberGenerator`
- ✅ Удалён `isValidLuhn` из `CardNumberGenerator`

## Что осталось

### Step 1: Удалить закомментированный код
- `PersonMapper.java:14-16` — удалить закомментированные `toPersonCreateRequest` и `toEntity(PersonResponse)`
- `CardMapper.java:13-14` — удалить закомментированный `toCardCreateRequest`

### Step 2: Подключить маскирование номера карты
1. **`CardResponse.java`** — добавить поле `String maskedNumber`
2. **`CardMapper.java`** — добавить `@Mapping(target = "maskedNumber", ignore = true)` (значение вычисляется в сервисе)
3. **`CardService.java`** — инжектить `CardMaskUtil` + `CardEncryptionUtil`, в `getCardById` и `getCardsByPerson` расшифровывать + маскировать номер перед маппингом

### Step 3: Обновить тесты
- `CardServiceTest` — добавить моки для `CardMaskUtil`
- `CardControllerTest` — добавить `maskedNumber` в ожидаемые ответы

---

## Files

| File | Change |
|------|--------|
| `mapper/PersonMapper.java` | Удалить закомментированный код |
| `mapper/CardMapper.java` | Удалить закомментированный код, добавить `@Mapping(ignore)` для `maskedNumber` |
| `dto/card/CardResponse.java` | Добавить `maskedNumber` |
| `service/CardService.java` | Подключить `CardMaskUtil` + `CardEncryptionUtil`, маскировать при чтении |
| `service/CardServiceTest.java` | Обновить моки |
| `controller/CardControllerTest.java` | Обновить ожидания |

## Verification
```bash
./gradlew test && ./gradlew build
```
