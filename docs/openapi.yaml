openapi: "3.0.3"
info:
  title: "OpenAPI definition"
  description: "bank_rest API"
  version: "1.0.0"
servers:
  - url: "http://localhost:8080"
    description: "Generated server url"
paths:
  /api/admin/block-requests/pending:
    get:
      tags:
        - "admin-card-block-request-controller"
      operationId: "getPendingRequests"
      parameters:
        - name: "pageable"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Pageable"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PageCardBlockRequestAdminResponse"
  /api/admin/block-requests/{id}/approve:
    patch:
      tags:
        - "admin-card-block-request-controller"
      operationId: "approveRequest"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardBlockRequestAdminResponse"
  /api/admin/block-requests/{id}/reject:
    patch:
      tags:
        - "admin-card-block-request-controller"
      operationId: "rejectRequest"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardBlockRequestAdminResponse"
  /api/admin/cards:
    get:
      tags:
        - "admin-card-controller"
      operationId: "getAllCards"
      parameters:
        - name: "personId"
          in: "query"
          required: false
          schema:
            type: "string"
            format: "uuid"
        - name: "status"
          in: "query"
          required: false
          schema:
            type: "string"
        - name: "pageable"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Pageable"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PageCardAdminResponse"
    post:
      tags:
        - "admin-card-controller"
      operationId: "createCard"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardCreateRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardAdminResponse"
  /api/admin/cards/{id}:
    get:
      tags:
        - "admin-card-controller"
      operationId: "getCardById"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardAdminResponse"
    delete:
      tags:
        - "admin-card-controller"
      operationId: "deleteCard"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "204":
          description: "No Content"
  /api/admin/cards/{id}/block:
    patch:
      tags:
        - "admin-card-controller"
      operationId: "blockCard"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardAdminResponse"
  /api/admin/cards/{id}/activate:
    patch:
      tags:
        - "admin-card-controller"
      operationId: "activateCard"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardAdminResponse"
  /api/admin/transfers:
    get:
      tags:
        - "admin-transfer-history-controller"
      operationId: "getAllTransfers"
      parameters:
        - name: "personId"
          in: "query"
          required: false
          schema:
            type: "string"
            format: "uuid"
        - name: "pageable"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Pageable"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PageTransferHistoryResponse"
  /api/admin/users:
    get:
      tags:
        - "admin-user-controller"
      operationId: "getAllUsers"
      parameters:
        - name: "pageable"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Pageable"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PagePersonAdminResponse"
    post:
      tags:
        - "admin-user-controller"
      operationId: "createUser"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonCreateRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PersonAdminResponse"
  /api/admin/users/{id}:
    get:
      tags:
        - "admin-user-controller"
      operationId: "getUserById"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PersonAdminResponse"
    put:
      tags:
        - "admin-user-controller"
      operationId: "updateUser"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonUpdateRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PersonAdminResponse"
    delete:
      tags:
        - "admin-user-controller"
      operationId: "deleteUser"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "204":
          description: "No Content"
  /api/auth/login:
    post:
      tags:
        - "auth-controller"
      operationId: "login"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/auth/refresh:
    post:
      tags:
        - "auth-controller"
      operationId: "refresh"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/cards/person/{personId}:
    get:
      tags:
        - "user-card-controller"
      operationId: "getMyCards"
      parameters:
        - name: "personId"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
        - name: "status"
          in: "query"
          required: false
          schema:
            type: "string"
        - name: "pageable"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Pageable"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/PageCardResponse"
  /api/cards/{id}:
    get:
      tags:
        - "user-card-controller"
      operationId: "getMyCardById"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardResponse"
  /api/cards/{id}/block-request:
    put:
      tags:
        - "user-card-controller"
      operationId: "requestBlock"
      parameters:
        - name: "id"
          in: "path"
          required: true
          schema:
            type: "string"
            format: "uuid"
        - name: "authentication"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Authentication"
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardBlockRequestResponse"
  /api/transfers:
    post:
      tags:
        - "user-transfer-controller"
      operationId: "transfer"
      parameters:
        - name: "authentication"
          in: "query"
          required: true
          schema:
            $ref: "#/components/schemas/Authentication"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardTransferRequest"
        required: true
      responses:
        "200":
          description: "OK"
          content:
            '*/*':
              schema:
                $ref: "#/components/schemas/CardTransferResponse"
components:
  schemas:
    Sort:
      type: "object"
      properties:
        unsorted:
          type: "boolean"
        sorted:
          type: "boolean"
        empty:
          type: "boolean"
    PageableObject:
      type: "object"
      properties:
        paged:
          type: "boolean"
        pageNumber:
          type: "integer"
          format: "int32"
        offset:
          type: "integer"
          format: "int64"
        pageSize:
          type: "integer"
          format: "int32"
        unpaged:
          type: "boolean"
        sort:
          $ref: "#/components/schemas/Sort"
    CardBlockRequestAdminResponse:
      type: "object"
      properties:
        id:
          type: "integer"
          format: "int64"
        cardId:
          type: "string"
          format: "uuid"
        maskedCardNumber:
          type: "string"
        personId:
          type: "string"
          format: "uuid"
        blockRequestStatus:
          type: "string"
          enum:
            - "PENDING"
            - "APPROVED"
            - "REJECTED"
        createdDate:
          type: "string"
          format: "date-time"
        lastModifiedDate:
          type: "string"
          format: "date-time"
        createdBy:
          type: "string"
          format: "uuid"
        modifiedBy:
          type: "string"
          format: "uuid"
    PageCardBlockRequestAdminResponse:
      type: "object"
      properties:
        number:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        numberOfElements:
          type: "integer"
          format: "int32"
        last:
          type: "boolean"
        totalPages:
          type: "integer"
          format: "int32"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        sort:
          $ref: "#/components/schemas/Sort"
        content:
          type: "array"
          items:
            $ref: "#/components/schemas/CardBlockRequestAdminResponse"
        first:
          type: "boolean"
        totalElements:
          type: "integer"
          format: "int64"
        empty:
          type: "boolean"
    Pageable:
      type: "object"
      properties:
        page:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        sort:
          type: "array"
          items:
            type: "string"
    BigDecimal:
      type: "object"
      properties: { }
    CardAdminResponse:
      type: "object"
      properties:
        id:
          type: "string"
          format: "uuid"
        personId:
          type: "string"
          format: "uuid"
        personName:
          type: "string"
        maskedNumber:
          type: "string"
        expirationDate:
          type: "string"
          format: "date"
        cardStatus:
          type: "string"
          enum:
            - "ACTIVE"
            - "BLOCKED"
            - "EXPIRED"
        balance:
          $ref: "#/components/schemas/BigDecimal"
        createdDate:
          type: "string"
          format: "date-time"
        lastModifiedDate:
          type: "string"
          format: "date-time"
        createdBy:
          type: "string"
          format: "uuid"
        modifiedBy:
          type: "string"
          format: "uuid"
    PageCardAdminResponse:
      type: "object"
      properties:
        number:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        numberOfElements:
          type: "integer"
          format: "int32"
        last:
          type: "boolean"
        totalPages:
          type: "integer"
          format: "int32"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        sort:
          $ref: "#/components/schemas/Sort"
        content:
          type: "array"
          items:
            $ref: "#/components/schemas/CardAdminResponse"
        first:
          type: "boolean"
        totalElements:
          type: "integer"
          format: "int64"
        empty:
          type: "boolean"
    CardCreateRequest:
      type: "object"
      properties:
        personId:
          type: "string"
          format: "uuid"
        expirationDate:
          type: "string"
          format: "date"
        balance:
          $ref: "#/components/schemas/BigDecimal"
    TransferHistoryResponse:
      type: "object"
      properties:
        id:
          type: "integer"
          format: "int64"
        personId:
          type: "string"
          format: "uuid"
        fromCardId:
          type: "string"
          format: "uuid"
        toCardId:
          type: "string"
          format: "uuid"
        amount:
          $ref: "#/components/schemas/BigDecimal"
        transactionDate:
          type: "string"
          format: "date-time"
    PageTransferHistoryResponse:
      type: "object"
      properties:
        number:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        numberOfElements:
          type: "integer"
          format: "int32"
        last:
          type: "boolean"
        totalPages:
          type: "integer"
          format: "int32"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        sort:
          $ref: "#/components/schemas/Sort"
        content:
          type: "array"
          items:
            $ref: "#/components/schemas/TransferHistoryResponse"
        first:
          type: "boolean"
        totalElements:
          type: "integer"
          format: "int64"
        empty:
          type: "boolean"
    PersonAdminResponse:
      type: "object"
      properties:
        id:
          type: "string"
          format: "uuid"
        name:
          type: "string"
        role:
          type: "string"
          enum:
            - "ADMIN"
            - "USER"
        createdDate:
          type: "string"
          format: "date-time"
        lastModifiedDate:
          type: "string"
          format: "date-time"
        createdBy:
          type: "string"
          format: "uuid"
        modifiedBy:
          type: "string"
          format: "uuid"
    PagePersonAdminResponse:
      type: "object"
      properties:
        number:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        numberOfElements:
          type: "integer"
          format: "int32"
        last:
          type: "boolean"
        totalPages:
          type: "integer"
          format: "int32"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        sort:
          $ref: "#/components/schemas/Sort"
        content:
          type: "array"
          items:
            $ref: "#/components/schemas/PersonAdminResponse"
        first:
          type: "boolean"
        totalElements:
          type: "integer"
          format: "int64"
        empty:
          type: "boolean"
    PersonCreateRequest:
      type: "object"
      properties:
        name:
          type: "string"
        password:
          type: "string"
    PersonUpdateRequest:
      type: "object"
      properties:
        password:
          type: "string"
        role:
          type: "string"
          enum:
            - "ADMIN"
            - "USER"
    AuthRequest:
      type: "object"
      properties:
        name:
          type: "string"
        password:
          type: "string"
    AuthResponse:
      type: "object"
      properties:
        accessToken:
          type: "string"
        refreshToken:
          type: "string"
    RefreshRequest:
      type: "object"
      properties:
        refreshToken:
          type: "string"
    CardResponse:
      type: "object"
      properties:
        id:
          type: "string"
          format: "uuid"
        maskedNumber:
          type: "string"
        expirationDate:
          type: "string"
          format: "date"
        cardStatus:
          type: "string"
          enum:
            - "ACTIVE"
            - "BLOCKED"
            - "EXPIRED"
        balance:
          $ref: "#/components/schemas/BigDecimal"
    PageCardResponse:
      type: "object"
      properties:
        number:
          type: "integer"
          format: "int32"
        size:
          type: "integer"
          format: "int32"
        numberOfElements:
          type: "integer"
          format: "int32"
        last:
          type: "boolean"
        totalPages:
          type: "integer"
          format: "int32"
        pageable:
          $ref: "#/components/schemas/PageableObject"
        sort:
          $ref: "#/components/schemas/Sort"
        content:
          type: "array"
          items:
            $ref: "#/components/schemas/CardResponse"
        first:
          type: "boolean"
        totalElements:
          type: "integer"
          format: "int64"
        empty:
          type: "boolean"
    CardBlockRequestResponse:
      type: "object"
      properties:
        maskedCardNumber:
          type: "string"
        blockRequestStatus:
          type: "string"
          enum:
            - "PENDING"
            - "APPROVED"
            - "REJECTED"
    Object:
      type: "object"
      properties: { }
    Authentication:
      type: "object"
      properties:
        principal:
          $ref: "#/components/schemas/Object"
        authenticated:
          type: "boolean"
        credentials:
          $ref: "#/components/schemas/Object"
        name:
          type: "string"
        details:
          $ref: "#/components/schemas/Object"
        authorities:
          type: "array"
          items:
            type: "object"
    CardTransferRequest:
      type: "object"
      properties:
        fromCardId:
          type: "string"
          format: "uuid"
        toCardId:
          type: "string"
          format: "uuid"
        amount:
          $ref: "#/components/schemas/BigDecimal"
    CardTransferResponse:
      type: "object"
      properties:
        fromCardId:
          type: "string"
          format: "uuid"
        toCardId:
          type: "string"
          format: "uuid"
        amount:
          $ref: "#/components/schemas/BigDecimal"
        newFromBalance:
          $ref: "#/components/schemas/BigDecimal"
        newToBalance:
          $ref: "#/components/schemas/BigDecimal"